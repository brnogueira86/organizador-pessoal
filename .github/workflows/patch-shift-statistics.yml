name: Patch shift statistics
on:
  push:
    paths:
      - '.github/workflows/patch-shift-statistics.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply patch
        run: python scripts/patch_shift_statistics.py
      - name: Commit result
        run: |
          git config user.name "ChatGPT actions"
          git config user.email "actions@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Adicionar estatísticas históricas de plantões"
          git push
