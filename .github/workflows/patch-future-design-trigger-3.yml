name: Patch future design 3
on:
  push:
    paths:
      - '.github/workflows/patch-future-design-trigger-3.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Aplicar redesign
        run: python scripts/patch_future_design.py
      - name: Salvar alteração
        run: |
          git config user.name "ChatGPT actions"
          git config user.email "actions@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Redesign futurista de todo o Abel Ferreira"
          git push
